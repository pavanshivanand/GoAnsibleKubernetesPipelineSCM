apiVersion: v1
kind: Service
metadata:
  name: goansiblekubernetespipelinescm
spec:
  type: LoadBalancer
  selector:
    app: goansiblekubernetespipelinescm
  ports:
  - port: 80
    targetPort: 8080
